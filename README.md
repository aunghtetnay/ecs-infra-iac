`` Deploy ECR image to ECS Fargate using terraform ``

## Requirement installation
1. install terraform and aws-cli 
2. Generate aws access key and secrete key 

### Run this command 
    . terraform init
    . terraform plan 
    . terraform apply --auto-approve
